[package]
name = "pedalbox-telemetry"
version = "0.1.0"
edition = "2024"
authors.workspace = true
description.workspace = true

[dependencies]
postcard = "1.1.3"
serde = { version = "1.0.228", default-features = false, features = ["derive"] }

[dev-dependencies]
postcard = { version = "1.1.3", features = ["use-std"] }
